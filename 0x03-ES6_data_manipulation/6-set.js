// Creates and returns a Set from the elements of the provided array.
// Preserves only unique values, eliminating any duplicate entries in the resulting Set.
export default function setFromArray(arr) {
  return new Set(arr);
}
